# Copy to .env (or .env.production) and fill in.
# In dev you can leave this unset — Vite proxies /api → http://localhost:8080.
# In prod, point this at the deployed backend (must include the /api suffix).
VITE_API_URL=https://your-backend.onrender.com/api
